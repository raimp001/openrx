[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tao-autoresearch"
version = "0.1.0"
description = "Minimal TAO strategy autoresearch harness inspired by karpathy/autoresearch."
requires-python = ">=3.9"
dependencies = []
